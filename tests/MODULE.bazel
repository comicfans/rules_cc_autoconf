bazel_dep(name = "rules_cc", version = "0.2.17")
bazel_dep(name = "rules_cc_autoconf", version = "0.15.0")

bazel_dep(name = "meson_config", version = "0.0.0")
local_path_override(
    module_name = "meson_config",
    path = "../",
)
